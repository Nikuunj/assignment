generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("KEY_DB1")
}

model privateKey {
  pubkey      String         @id
  pvtKey      Bytes
}